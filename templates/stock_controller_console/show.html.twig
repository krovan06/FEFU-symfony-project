{% extends 'base.html.twig' %}

{% block title %}Stock{% endblock %}

{% block body %}
    <h1>Stock</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stock.id }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ stock.Stock }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stock_controller_console_index') }}">back to list</a>

    <a href="{{ path('app_stock_controller_console_edit', {'id': stock.id}) }}">edit</a>

    {{ include('stock_controller_console/_delete_form.html.twig') }}
{% endblock %}
